[project]
name = "apk-build"
version = "0.1.0"
description = "APK builder"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
apk = "apk_build:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
