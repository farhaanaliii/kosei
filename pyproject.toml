[project]
name = "kosei"
version = "0.1.0"
description = "Compiles, packages, and signs Android APKs directly on Android devices via Termux"
readme = "README.md"
requires-python = ">=3.7"
dependencies = []

[project.scripts]
kosei = "kosei:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
